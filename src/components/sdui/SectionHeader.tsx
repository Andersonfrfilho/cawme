import { View, Text, TouchableOpacity } from 'react-native'; import { SduiComponentProps } from '@/types/bff/sdui.types'; export default function SectionHeader({ data, config, onItemPress }: SduiComponentProps) { return ( <TouchableOpacity onPress={() => onItemPress()} style={{ padding: 10, margin: 5, backgroundColor: '#f0f0f0', borderRadius: 8 }}><Text style={{ fontWeight: 'bold' }}>SectionHeader</Text><Text>{JSON.stringify(config)}</Text></TouchableOpacity> ); }

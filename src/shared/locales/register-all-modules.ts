/**
 * Registro de locales de módulos por side-effect.
 *
 * ⚠️ Metro/Expo não suporta `require` dinâmico (ex.: `require(p)`).
 * Sempre use imports estáticos neste arquivo.
 *
 * Exemplo quando existir um locale de módulo:
 * import "@/modules/chat/locales";
 */

import "@/modules/auth/locales";

import { View, Text, TouchableOpacity } from 'react-native'; import { styles } from './styles'; import { SduiComponentProps } from '@/modules/sdui/types/sdui.types'; export default function SearchFilters({ data, config, onItemPress }: SduiComponentProps) { return ( <TouchableOpacity onPress={() => onItemPress()} style={styles.card}><Text style={styles.cardTitle}>SearchFilters</Text><Text>{JSON.stringify(config)}</Text></TouchableOpacity> ); }

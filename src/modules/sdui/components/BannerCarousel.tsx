import { View, Text, TouchableOpacity } from 'react-native'; import { styles } from './styles'; import { SduiComponentProps } from '@/modules/sdui/types/sdui.types'; export default function BannerCarousel({ data, config, onItemPress }: SduiComponentProps) { return ( <TouchableOpacity onPress={() => onItemPress()} style={styles.card}><Text style={styles.cardTitle}>BannerCarousel</Text><Text>{JSON.stringify(config)}</Text></TouchableOpacity> ); }

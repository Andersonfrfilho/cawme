import { registerLocaleModule } from "@/shared/locales";

export const sduiLocale = {
  sdui: {
    emptyState: "Nenhum conteúdo disponível",
    retry: "Tentar novamente",
  },
};

registerLocaleModule(sduiLocale);

export default sduiLocale;
